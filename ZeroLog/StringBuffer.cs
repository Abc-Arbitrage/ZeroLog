using System.Text;

namespace ZeroLog
{
    internal class StringBuffer
    {
        public int Count { get; private set; }

        public int CopyTo(byte[] destination, int sourceIndex, int count, Encoding encoding)
        {
            throw new System.NotImplementedException();
        }
    }
}